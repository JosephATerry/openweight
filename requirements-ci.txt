# CI extends the production image dependency set without installing CUDA or
# the isolated QLoRA training environment.
-r requirements-container.txt

pytest==9.1.1
PyYAML==6.0.3
pip-audit==2.10.1
detect-secrets==1.5.0
